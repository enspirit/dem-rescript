/* Functions of the command-line interface of dem-rescript (doc-e-mate in ReScript) are not meant to be
 * consumed by clients, so the whole code is private, and the cli exposes its commands in its own manual
 * accessible through the --help command. */
